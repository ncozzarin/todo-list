/* function addTask(){
    setState({
      taskCount: state.taskCount + 1
    })
    const tasks = [];
      for (let i = 0; i < state.taskCount; i ++){
        tasks.push(<Task key={i} number={i}/>);
      }
      return(
        <div>
          <input type="checkbox"/>
          <p>{state.task}</p>
        </div>
      );
    } */